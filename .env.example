# Loke Cards Environment Configuration

# Production URL (fixed - installed in macOS dock)
VITE_APP_URL=https://loke.tail2d448.ts.net:8443

# Loke Engine API URL (backend server)
VITE_LOKE_ENGINE_API=https://loke.tail2d448.ts.net:3000

# Project name (optional - can be set per instance)
VITE_PROJECT_NAME=my-adventure

# Server configuration
# Vite runs HTTPS on port 8080, Tailscale serves on /cards path
