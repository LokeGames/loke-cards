# Loke Cards Environment Configuration

# Loke Engine API URL (backend server)
VITE_LOKE_ENGINE_API=https://loke-engine:3000

# Project name (optional - can be set per instance)
VITE_PROJECT_NAME=my-adventure

# Server configuration
# Vite will use HTTPS with self-signed cert on port 443
